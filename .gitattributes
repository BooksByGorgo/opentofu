# Every text file is stored and checked out with LF, on every platform.
# Without this, core.autocrlf=true on Windows checks files out with CRLF, and
# the sayings module splits its word lists on "\n" alone, leaving a stray
# carriage return in every prefix and suffix.
* text=auto eol=lf

# Binary: never touched.
*.png binary
