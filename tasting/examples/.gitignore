# OpenTofu/Terraform working files. Lock files are ignored here because
# these examples are meant to be run on any platform; commit them in real
# projects (see Appendix A).
**/.terraform/
**/.terraform.lock.hcl
**/*.tfstate
**/*.tfstate.*
**/*.tfvars
!**/example.tfvars
**/crash.log
# generated by the chapter 3 config
database/seed/
